// Register `phoneList` component, along with its associated controller and template
angular.
module('phonecatApp').component('rowanIt', {
    template:
    '<button ng-click="$ctrl.meth(1)">buy one</button>' ,
    bindings: { meth:'<'}




});